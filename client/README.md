## Instructions

1. After cloning the repo, download dependencies
   - Node.js: npm install
   - React: cd client yarn
2. Once dependencies are successfully downloaded, run 'npm start' from the root of the folder
